1) Add confidence interval for accuracy of input distribution provided to the model.
2) note that the current implimentation do not alow strategy() to use random as it messes with evaluate_stratergy() as it runs it with the same seed for different stratergies.
    To fix that, we will need to modify rating_game.py simulate to take in an additional argument senario = [None, 4, 5, None, None, ...]
3) THE PRACTICAL PART.  suppose we have a senario, give it to strategy and tell us what to do.
[project]
name = "math"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "jthecontrarian", email = "jthecontrarian@proton.me" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
math = "math:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
